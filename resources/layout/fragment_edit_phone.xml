<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="@color/white">

    <TextView
        android:id="@+id/my_number_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/bound_number"
        android:textColor="@color/grey"
        android:textSize="15sp"
        android:padding="10dp"
        android:layout_marginBottom="20dp"/>

    <EditText
        android:id="@+id/my_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/round_lightgrey_background"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:hint="@string/register_a_number"
        android:textColorHint="@color/light_grey"
        android:textSize="24sp"
        android:textColor="@color/dark_grey"
        android:gravity="center"
        android:paddingTop="30dp"
        android:paddingBottom="30dp"
        android:layout_below="@+id/my_number_title"
        android:maxLines="1"
        android:singleLine="true" />

    <Button
        android:id="@+id/change_number"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:text="@string/change_number"
        android:layout_marginTop="25dp"
        android:textStyle="bold"
        style="@style/red_button"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>